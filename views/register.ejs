<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <!-- Bootstrap CSS -->
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css">
        <title>TinyApp</title>
    </head>
    <body>
        
        <%- include("partials/_header") %>
        <div class="m-2 p-2" style="display: flex; align-items: center; justify-content:center">
            <form action="/register?_method=PUT" method="POST">
                <br>
                <h3 class="d-flex justify-content-center">Register</h3>
                <br>
                <input class="form-control m-2" type="email" name="email" placeholder="Email">
                <input class="form-control m-2" type="password" name="password" placeholder="Password">
                <div style="display: flex; align-items: center; justify-content:center">
                    <button type="submit" class="btn btn-primary m-2">Register</button>
                </div>
            </form>
        </div>
    </body>
</html>